module.exports = {
    // Decay rate on cred over time
    // 0 means consider all weeks equally
    // 0.X creates an exponential decay
    // 1 means only consider current week
    DECAY: 0,
    GUILD_IDS: {
        "798975165342023700": "sourcecred/cred/gh-pages"
    },
}