"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SimpleFormIterator = void 0;
var React = __importStar(require("react"));
var react_1 = require("react");
var core_1 = require("@material-ui/core");
var styles_1 = require("@material-ui/core/styles");
var AddCircleOutline_1 = __importDefault(require("@material-ui/icons/AddCircleOutline"));
var RemoveCircleOutline_1 = __importDefault(require("@material-ui/icons/RemoveCircleOutline"));
var ArrowUpward_1 = __importDefault(require("@material-ui/icons/ArrowUpward"));
var ArrowDownward_1 = __importDefault(require("@material-ui/icons/ArrowDownward"));
var classnames_1 = __importDefault(require("classnames"));
var get_1 = __importDefault(require("lodash/get"));
var prop_types_1 = __importDefault(require("prop-types"));
var ra_core_1 = require("ra-core");
var react_transition_group_1 = require("react-transition-group");
var button_1 = require("../button");
var FormInput_1 = __importDefault(require("./FormInput"));
exports.SimpleFormIterator = function (props) {
    var _a = props.addButton, addButton = _a === void 0 ? React.createElement(DefaultAddButton, null) : _a, _b = props.removeButton, removeButton = _b === void 0 ? React.createElement(DefaultRemoveButton, null) : _b, _c = props.reOrderButtons, reOrderButtons = _c === void 0 ? React.createElement(DefaultReOrderButtons, null) : _c, basePath = props.basePath, children = props.children, className = props.className, fields = props.fields, _d = props.meta, error = _d.error, submitFailed = _d.submitFailed, record = props.record, resource = props.resource, source = props.source, disabled = props.disabled, disableAdd = props.disableAdd, disableRemove = props.disableRemove, disableReordering = props.disableReordering, variant = props.variant, margin = props.margin, TransitionProps = props.TransitionProps, defaultValue = props.defaultValue, _e = props.getItemLabel, getItemLabel = _e === void 0 ? DefaultLabelFn : _e;
    var classes = useStyles(props);
    var nodeRef = react_1.useRef(null);
    // We need a unique id for each field for a proper enter/exit animation
    // so we keep an internal map between the field position and an auto-increment id
    var nextId = react_1.useRef(fields && fields.length
        ? fields.length
        : defaultValue
            ? defaultValue.length
            : 0);
    // We check whether we have a defaultValue (which must be an array) before checking
    // the fields prop which will always be empty for a new record.
    // Without it, our ids wouldn't match the default value and we would get key warnings
    // on the CssTransition element inside our render method
    var ids = react_1.useRef(nextId.current > 0 ? Array.from(Array(nextId.current).keys()) : []);
    var removeField = function (index) { return function () {
        ids.current.splice(index, 1);
        fields === null || fields === void 0 ? void 0 : fields.remove(index);
    }; };
    // Returns a boolean to indicate whether to disable the remove button for certain fields.
    // If disableRemove is a function, then call the function with the current record to
    // determining if the button should be disabled. Otherwise, use a boolean property that
    // enables or disables the button for all of the fields.
    var disableRemoveField = function (record, disableRemove) {
        if (typeof disableRemove === 'boolean') {
            return disableRemove;
        }
        return disableRemove && disableRemove(record);
    };
    var addField = function () {
        ids.current.push(nextId.current++);
        fields === null || fields === void 0 ? void 0 : fields.push(undefined);
    };
    // add field and call the onClick event of the button passed as addButton prop
    var handleAddButtonClick = function (originalOnClickHandler) { return function (event) {
        addField();
        if (originalOnClickHandler) {
            originalOnClickHandler(event);
        }
    }; };
    // remove field and call the onClick event of the button passed as removeButton prop
    var handleRemoveButtonClick = function (originalOnClickHandler, index) { return function (event) {
        removeField(index)();
        if (originalOnClickHandler) {
            originalOnClickHandler(event);
        }
    }; };
    var handleReorder = function (origin, destination) {
        var item = ids.current[origin];
        ids.current[origin] = ids.current[destination];
        ids.current[destination] = item;
        fields === null || fields === void 0 ? void 0 : fields.move(origin, destination);
    };
    var records = get_1.default(record, source);
    return fields ? (React.createElement("ul", { className: classnames_1.default(classes.root, className) },
        submitFailed && typeof error !== 'object' && error && (React.createElement(core_1.FormHelperText, { error: true },
            React.createElement(ra_core_1.ValidationError, { error: error }))),
        React.createElement(react_transition_group_1.TransitionGroup, { component: null }, fields.map(function (member, index) { return (React.createElement(react_transition_group_1.CSSTransition, __assign({ nodeRef: nodeRef, key: ids.current[index], timeout: 500, classNames: "fade" }, TransitionProps),
            React.createElement("li", { className: classes.line },
                React.createElement("div", null,
                    React.createElement("div", { className: classes.indexContainer },
                        React.createElement(core_1.Typography, { variant: "body1", className: classes.index }, getItemLabel(index)),
                        !disabled &&
                            !disableReordering &&
                            react_1.cloneElement(reOrderButtons, {
                                index: index,
                                max: fields.length,
                                onReorder: handleReorder,
                                className: classnames_1.default('button-reorder', "button-reorder-" + source + "-" + index),
                            }))),
                React.createElement("section", { className: classes.form }, react_1.Children.map(children, function (input, index2) {
                    if (!react_1.isValidElement(input)) {
                        return null;
                    }
                    var _a = input.props, source = _a.source, inputProps = __rest(_a, ["source"]);
                    return (React.createElement(FormInput_1.default, { basePath: input.props.basePath ||
                            basePath, input: react_1.cloneElement(input, __assign({ source: source
                                ? member + "." + source
                                : member, index: source
                                ? undefined
                                : index2, label: typeof input.props
                                .label ===
                                'undefined'
                                ? source
                                    ? "resources." + resource + ".fields." + source
                                    : undefined
                                : input.props.label, disabled: disabled }, inputProps)), record: (records &&
                            records[index]) ||
                            {}, resource: resource, variant: variant, margin: margin }));
                })),
                !disabled &&
                    !disableRemoveField((records && records[index]) || {}, disableRemove) && (React.createElement("span", { className: classes.action }, react_1.cloneElement(removeButton, {
                    onClick: handleRemoveButtonClick(removeButton.props.onClick, index),
                    className: classnames_1.default('button-remove', "button-remove-" + source + "-" + index),
                })))))); })),
        !disabled && !disableAdd && (React.createElement("li", { className: classes.line },
            React.createElement("span", { className: classes.action }, react_1.cloneElement(addButton, {
                onClick: handleAddButtonClick(addButton.props.onClick),
                className: classnames_1.default('button-add', "button-add-" + source),
            })))))) : null;
};
exports.SimpleFormIterator.defaultProps = {
    disableAdd: false,
    disableRemove: false,
};
exports.SimpleFormIterator.propTypes = {
    defaultValue: prop_types_1.default.any,
    addButton: prop_types_1.default.element,
    removeButton: prop_types_1.default.element,
    basePath: prop_types_1.default.string,
    children: prop_types_1.default.node,
    classes: prop_types_1.default.object,
    className: prop_types_1.default.string,
    // @ts-ignore
    fields: prop_types_1.default.object,
    meta: prop_types_1.default.object,
    // @ts-ignore
    record: prop_types_1.default.object,
    source: prop_types_1.default.string,
    resource: prop_types_1.default.string,
    translate: prop_types_1.default.func,
    disableAdd: prop_types_1.default.bool,
    disableRemove: prop_types_1.default.oneOfType([prop_types_1.default.func, prop_types_1.default.bool]),
    TransitionProps: prop_types_1.default.shape({}),
};
var useStyles = styles_1.makeStyles(function (theme) {
    var _a, _b;
    return ({
        root: {
            padding: 0,
            marginBottom: 0,
            '& > li:last-child': {
                borderBottom: 'none',
            },
        },
        line: (_a = {
                display: 'flex',
                listStyleType: 'none',
                borderBottom: "solid 1px " + theme.palette.divider
            },
            _a[theme.breakpoints.down('xs')] = { display: 'block' },
            _a['&.fade-enter'] = {
                opacity: 0.01,
                transform: 'translateX(100vw)',
            },
            _a['&.fade-enter-active'] = {
                opacity: 1,
                transform: 'translateX(0)',
                transition: 'all 500ms ease-in',
            },
            _a['&.fade-exit'] = {
                opacity: 1,
                transform: 'translateX(0)',
            },
            _a['&.fade-exit-active'] = {
                opacity: 0.01,
                transform: 'translateX(100vw)',
                transition: 'all 500ms ease-in',
            },
            _a),
        index: (_b = {},
            _b[theme.breakpoints.down('sm')] = { display: 'none' },
            _b.marginRight = theme.spacing(1),
            _b),
        indexContainer: {
            display: 'flex',
            paddingTop: '1em',
            marginRight: theme.spacing(1),
            alignItems: 'center',
        },
        form: { flex: 2 },
        action: {
            paddingTop: '0.5em',
        },
        leftIcon: {
            marginRight: theme.spacing(1),
        },
    });
}, { name: 'RaSimpleFormIterator' });
var DefaultAddButton = function (props) {
    var classes = useStyles(props);
    var translate = ra_core_1.useTranslate();
    return (React.createElement(core_1.Button, __assign({ size: "small" }, props),
        React.createElement(AddCircleOutline_1.default, { className: classes.leftIcon }),
        translate('ra.action.add')));
};
var DefaultLabelFn = function (index) { return index + 1; };
var DefaultRemoveButton = function (props) {
    var classes = useStyles(props);
    var translate = ra_core_1.useTranslate();
    return (React.createElement(core_1.Button, __assign({ size: "small" }, props),
        React.createElement(RemoveCircleOutline_1.default, { className: classes.leftIcon }),
        translate('ra.action.remove')));
};
var DefaultReOrderButtons = function (_a) {
    var className = _a.className, index = _a.index, max = _a.max, onReorder = _a.onReorder;
    return (React.createElement("div", { className: className },
        React.createElement(button_1.IconButtonWithTooltip, { label: "ra.action.move_up", size: "small", onClick: function () { return onReorder(index, index - 1); }, disabled: index <= 0 },
            React.createElement(ArrowUpward_1.default, null)),
        React.createElement(button_1.IconButtonWithTooltip, { label: "ra.action.move_down", size: "small", onClick: function () { return onReorder(index, index + 1); }, disabled: max == null || index >= max - 1 },
            React.createElement(ArrowDownward_1.default, null))));
};
